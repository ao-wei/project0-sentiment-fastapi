name: proj0-nlp
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - transformers>=4.45.0
      - datasets
      - numpy
      - fastapi
      - uvicorn[standard]
      - pydantic>=2.0
      - streamlit
      - requests
